{
  "name": "guidance-zone-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd app && npm ci --include=dev && cd ../server && npm ci --include=dev",
    "build": "cd app && npm run build && cd ../server && npx prisma generate",
    "start": "cd server && node --import tsx src/index.ts",
    "dev": "concurrently \"cd server && npm run dev\" \"cd app && npm run dev\""
  }
}
